[{:date "2026-06-11"
  :rule "Every tests/*_test.clj must end with (run-tests-and-exit); a file without it silently runs zero tests when invoked standalone."
  :context "fix-bug rehearsal: ./mino tests/arithmetic_test.clj exited 0 with a planted defect because the footer is missing; 99 of the suite's test files share the gap. Candidate deterministic gate: a spine/qa check that greps test files for the footer."
  :applies-to :tests
  :suggested-home "deterministic gate (tests/run.clj preflight or qa-arch-style check)"}
 {:date "2026-06-11"
  :rule "qa-arch needs a committed baseline (like tools/analyze_baseline.txt) or ALLOW rationales for its 21 pre-existing failures, so the verify-lanes verdict is mechanical instead of judgment."
  :context "audit-code rehearsal: qa-arch fails on main (12 oversized TUs, 8 oversized functions, 1 abort without rationale), so every landing-wave verifier must be told the known set ad hoc and the haiku verifier waffled on the verdict. check-analyze-zig's baseline pattern is the template."
  :applies-to :toolchain
  :suggested-home "deterministic gate (qa-arch baseline file + check, TDD per write-clj)"}
 {:date "2026-06-12"
  :rule "str/index-of with a non-ASCII needle is pathologically slow (~9 s for one scan of a 205 KB haystack vs 19 ms for an ASCII needle); fix the slow path in the C implementation."
  :context "Changelog scrub tooling: a count-occurrences loop over CHANGELOG.md appeared hung; bisection showed each em-dash-containing index-of scan took seconds. Likely per-position UTF-8 decoding in the needle path. Repro: (str/index-of (slurp big-file) \"## v0.x — Title\")."
  :applies-to :c
  :suggested-home "fix-bug run on src/prim/string.c index-of; perf assertion in tests"}
 {:date "2026-06-12"
  :rule "load-file must restore the caller's namespace; in mino the loaded file's (ns ...) leaks and subsequent caller forms compile in the loaded namespace (JVM Clojure restores *ns*)."
  :context "Changelog scrub tooling: after (load-file \"x.clj\") where x.clj declares (ns retro.scrubpairs), the loading script's later forms failed with 'no such alias: str in namespace retro.scrubpairs'. Conformance gap vs JVM Clojure load/load-file semantics."
  :applies-to :c
  :suggested-home "fix-bug run on the require/load machinery (src/runtime/module.c or prim/module.c); conformance test"}]

# Architecture Baseline — 2026-04-21

Captured after completing evaluator decomposition (WS1), primitive modularization (WS2), and abort policy audit (WS4).

## Gate Configuration

- TU size limit: 1100 LOC
- Function size limit: 250 LOC
- Abort sites: all must have invariant-fatal rationale

## Translation Unit Sizes

42 source files in src/.

Allowlisted:

| File | LOC | Rationale |
|------|-----|-----------|
| src/read.c | 1477 | Lexer/parser, inherently sequential |
| src/prim_collections.c | 1104 | 14 domain primitives, barely over limit |

All other files under 1100 LOC. Largest non-allowlisted: src/prim_sequences.c (897), src/prim_string.c (804), src/eval_special.c (755).

## Function Sizes

Allowlisted:

| File | Function | LOC | Rationale |
|------|----------|-----|-----------|
| src/eval_special.c | eval_impl | 574 | Main evaluator dispatch |
| src/read.c | read_form | 363 | Reader dispatch |
| src/prim.c | mino_install_core | 289 | Primitive registration table |

All other functions under 250 LOC.

## Abort Inventory

11 abort() sites across 3 files. All classified as Class I (invariant-fatal).

| File | Sites | Context |
|------|-------|---------|
| src/prim.c | 5 | core.mino bootstrap failures (OOM, parse error, eval error) |
| src/runtime_gc.c | 5 | GC-internal OOM or corruption with no try frame |
| src/runtime_state.c | 1 | Initial state allocation failure |

All sites have inline rationale comments. No user-triggerable recoverable paths terminate via abort().

## Module Structure

- 5 evaluator files (dispatch + 4 domain modules)
- 14 primitive domain files + 1 shared infrastructure (prim.c)
- 5 runtime files (state, gc, env, error, var)
- 4 data structure files (val, vec, map, rbtree)
- 3 reader/printer/regex files
- 6 async files + 1 async primitive file
- 2 interop files (host_interop, clone)
- 1 diagnostics file

Headers: mino.h (public), mino_internal.h (core internals), eval_special_internal.h (evaluator cross-domain), prim_internal.h (primitive shared declarations), re.h (regex, standalone).
